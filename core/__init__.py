# This file makes the 'core' directory a Python package.

# You can selectively expose modules or symbols here if desired, e.g.:
# from .state import FractalState
# from .nlp import GodTierNLPFusion

# Or keep it empty to require full path imports like core.state.FractalState
# For OFRC, explicit full path imports are used, so this can be minimal.

# print("core package loaded.")
